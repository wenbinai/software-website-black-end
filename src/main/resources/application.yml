spring:
  application:
    name: software-website
  datasource:
    url: jdbc:mysql://localhost:3306/software-website?characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  jpa:
    show-sql: true

server:
  port: 80